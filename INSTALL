Follow the steps:

1. Clone the repository
   $ git clone https://github.com/araujotassia/login-logout.git

2. Load the database tables: create a database 'project', and run
   the provided SQL to create the needed tables (project.sql)

3. Edit the config.php file, change update the database credentials

4. Access the index.php file using a browser:
   http://localhost/login-logout/index.html
